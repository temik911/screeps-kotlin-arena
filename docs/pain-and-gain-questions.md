# Pain and Gain — открытые вопросы оператору (после доработок А–Д, 20.09.2026)

Состояние: пять доработок по итогам плана архитектуры посажены в `main` как v448–v452 (теги `pain-and-gain-v448` …
`pain-and-gain-v452`, `main` = a0f4da2). Ниже — то, что требует решения оператора, с рекомендацией исполнителя. Числа —
из абзацев v448–v452 в `docs/pain-and-gain.md`. Решение записывается сюда же строкой «Решено: …» и датой; закрытый пункт
переезжает в абзац версии, которая его исполнила.

1. **Находка Д: раздетый мили не считается своим мягким.** `guards(e)` (у `guardNow`) и признак «мягкий» в `poker`
   (`Tactician.kt`) остались на `!meleeOnlyBorn(a)`: рождённый мили без живой ATTACK своим мягким не считается, и когда
   его мили рубит нашего раздетого мили, ни `guards`, ни `poker` не срабатывают, `covered(e)` не открывает `engage`. Это
   единственные два места, где различие А/Б достижимо (846 крипо-тиков «раздетый мили в трёх от его вооружённого» на
   гейте). Менять ли на `!meleeOnlyLive(a)`?
   Рекомендация: да, отдельной поведенческой версией. По разбиению «тело — А, урон — Б» вопрос «кого защищаем» — о том,
   может ли жертва ответить, то есть Б. Гейт к этим местам экспонирован: гейт против `gate_452`, затем 8+8 против
   MetalicaX#10 и ricardo18informatica2020#13 с контролем v450 (4-4 и 4-4).
   Сделано (v472, прибор v473): гейт 122/140 со сдвигом в обе стороны (все 18 — от `poker`), A/B v471 против v473
   вперемежку против MetalicaX#12 — 5-3 и 5-3, правка действовала 25 крипо-тиков за 8 игр (`gstrip=0/25`); остаётся.
   Решено (20.09.2026, оператор: «выполняй по порядку»): да; в коде с v472, прибор с v473 — абзацы в `history-v4xx.md`.

2. **v450 на стенде отступает в 5,8 раза чаще**: posture.retreat 290 → 1 677 тиков, rung.retreat 1 382 → 3 567
   (`series.py reach --stand runs/gate_449` против `runs/gate_452`); живьём v450 не хуже контроля (4-4 / 4-4 против
   4-4 / 2-6). Оставлять v450 или вернуть притязания к семантике v449?
   Рекомендация: оставить. Знака на исходах нет ни на стенде, ни живьём, а отступление на стенде — то, к чему гейт до
   пункта А был слеп. Заложить проверку: после следующей рейтинговой серии сравнить доли постур живьём
   (`tools/series.py shares --control 447 --final <новая>`); если доля retreat живьём выросла без роста побед —
   вернуться к пункту.
   Проверено (20.09.2026, серия v477 из 7 рейтинговых по одной, вопрос 6): доля постуры RETREAT живьём 33,3 % (по играм
   0…81,4; выше 45 % в четырёх из семи) против 9,7 % у v447 (20 игр, 0…45,0) и 7,4 % у v461 (20 игр, 0…58,5); побед не
   прибавилось (2-5, рейтинг 1292 → 1263). Условие возврата выполнено по букве, но НЕ указывает на v450: серия v461
   уже содержит v450 и даёт 7,4 % — рост лежит между v461 и v477, то есть в v462–v475 (кандидаты по механизму: v462
   `escapeNeeded` у цели-флага, v474 `Prev.touchShareLast` в мере мощи, v472 `guards` / `poker`). Разрез `cmdwhy` той же
   серии: `retreat` 7,7 → 5,7 %, `posture` 1,7 → 6,4 %, `outmatched` 2,8 → 4,9 %; самая большая доля RETREAT — в
   выигранной игре против けろびー#19 (81,4 % за 1 900 тиков), то есть постура держится и вне контакта.
   Решено (20.09.2026): v450 остаётся; рост RETREAT между v461 и v477 — находка сверх списка, разрез по версиям
   v462–v475 нужен отдельным замером (A/B v461 против v477 на одном боте), решает оператор.

3. **Один содержательный проигрыш v450 на стенде** — `match13:brawl+heals`: на t=66 у мили #13 пропал приказ
   (`order/(46,43)` → `kite/stay`), строй развалился, net на +50 упал с +9 363 до +4 893, аннигиляция на 371-м стала
   победой по очкам. Разбирать ли причину?
   Рекомендация: сначала прибор, не правка — счётчик «бойцов без приказа после выбранной раздачи» (`unplaced=`, одна
   строка в `Commander.publishDeal`), затем по числу решать, единичный ли это случай.
   Сделано (v476): `unplaced=строевых/из них с кандидатом в шаге/лекарей/раздетых/раздач` в записи раздачи, тождество
   140/140. На гейте строевых без приказа 139 крипо-тиков (все — вне кулака, фермер и лагерь), с кандидатом в шаге —
   3 на 10 734 раздачи (0,03 %); `match13:brawl+heals` на нынешней сборке — 0.
   Решено (20.09.2026): случай единичный, причину не искать; прибор в строке, условие возврата — вторая часть
   `unplaced=` живьём больше единиц за матч.

4. **Старые поля приборов ворот `capgate=` / `cap=`** печатаются рядом с новыми (`capq=`, `capqu=`, `capu=`, `capeval=`,
   `capidle=`) до пересъёмки эталона. Когда снимать?
   Рекомендация: со следующей версией, которая и так переснимает эталон (например, версией из пункта 1), без отдельной
   пересъёмки. Старые разборы их не потеряют: новые поля печатаются с v451.
   Сделано (v477): сняты счётчики, замок `capSeen` / `capTick`, `capCount` и две записи `T_LINE`; compare 140/140, все
   140 логов побайтно те же без двух полей; эталон — `runs/gate_477`, снятый с этого прогона.
   Решено (20.09.2026): снято в v477.

5. **Проход `keeper` раздачи (Б.5)** — 0 клеток на 11 246 выбранных раздач гейта и 4 992 живых; недостижимость не
   доказывается. Оставить или удалить как редкое?
   Рекомендация: оставить, как `cmdwhy.outmatched`. Условие срабатывания названо и достижимо (боец с живым оружием или
   лечением на клетке нашего флага в шести и дальше клетках от медианы бойцов в режиме боя командира — хранитель, от
   которого армия ушла, вне режима пар); удаление сняло бы запасной путь и ничего не дало.
   Закрыто числом (v471, дефект 10): «0 клеток» был слепым счётчиком — `passKeeper` писал приказ мимо `commit`, где
   ведётся перепись проходов; через `commit` на том же гейте `pass=keeper` даёт 52 клетки в 6 сценариях.
   Решено (20.09.2026): оставить; проход достижим и виден прибору с v471 — абзац v471.

6. **Тестовый список** (`tools/play.py pain-and-gain --test-list`): ricardo18informatica2020#13 / #17,
   MetalicaX#9 / #10, けろびー#21. Coldkimchi#1 / #2 и MetalicaX#13 вытеснены серией v447. Возобновлять ли задачу
   «MetalicaX#13 или новее в тестовый список рейтинговыми играми по одной» (остановлена словом оператора 19.09.2026)?
   Рекомендация: да, но после пункта 1, чтобы не потерять нынешних ботов списка посреди замера. Строго по одной игре с
   проверкой `--test-list` после каждой: каждая рейтинговая игра сдвигает список.
   Сделано (20.09.2026, v477): 7 рейтинговых по одной с проверкой списка после каждой — 2-5 (победы над 欢欢#10 и
   けろびー#19; поражения от けろびー#17, MetalicaX#12 за 100 тиков, ricardo18informatica2020#14, MetalicaX#3, ●ω<♥♪#5 —
   все пять аннигиляции, три из них поздние, t=1500–1900 при достижимом его отрыве), рейтинг 1292 → 1263. MetalicaX
   встречен дважды — как #12 и как #3; #13 или новее в пуле не появился. После 7-й игры из списка выпал MetalicaX#12
   (бот, на котором мерились v462–v473), остался #3 — цикл остановлен, чтобы не потерять и его. Список после серии:
   けろびー#19, ●ω<♥♪#5, 欢欢#10, MetalicaX#3, ricardo18informatica2020#14.
   Решено (20.09.2026): задача #114 остаётся открытой — новее #12 у MetalicaX в пуле не видно; руки против него — на
   #3 (в списке), продолжать рейтинговые по одной — только по слову оператора (каждая игра вращает список).

7. **Следующий рычаг к победам.** С прошлого отрезка остался `threatAt` — цена клетки лекаря, знающая огонь следующего
   тика (`Forecast`, план переработки): ёмкость лечения на бойце под огнём 59 против его 102. Теперь есть и пункт 1. В
   каком порядке?
   Рекомендация: пункт 1 первым (дёшев, измерим), затем `threatAt` отдельной сборкой; её гейт теперь включает четыре
   ghost-записи отступления (пункт А), к которым правка лекарей в бою экспонирована.

8. **Семантика приборов раздачи с v449**: `pass=`, `gate=`, `fell=`, `flips=`, `adr=`, `hpick=`, `hadj=`, `hfire=`,
   `fall=`, `rotfm=` считают только ВЫБРАННУЮ раздачу, пробы замыслов — отдельно (`deals=выбрано/сыграно`, на гейте
   10 239 / 13 319); из-за этого `catchall` и `pinned` на выбранных раздачах дают 0 там, где по всем пробам давали 1–2.
   Устраивает ли, или нужен и разрез по пробам?
   Рекомендация: оставить как есть — в мир попадает выбранная раздача, её и мерить; долю проб даёт `deals=`.
   Решено (20.09.2026): оставить как есть; разреза по пробам нет.

9. **Внешний клон `~/ScreepsArena/screeps-arena-tools/`** (arukuka) с 19.09.2026 не используется: реплеи берутся
   через API (`tools/match-log.py replay`), формат описан там же. Удалять?
   Рекомендация: удалить.

## Постановка от 20.09.2026: поведенческие дефекты 1–14 — вопросы и состояние работы

Числа — из абзацев v462–v466 в `docs/pain-and-gain/history-v4xx.md`. Каждый пункт — отдельная версия под оракулом
`identity.sh`, эталон переснимается после принятой правки (`runs/gate_<версия>` в ворктри `pain-and-gain`).

10. **Пара «гарнизон + хранитель» (дефект 3, (д)).** Прибор `dblf=тот же флаг/разные` на гейте v464: 3 727 / 1 959
    крипо-тиков в 12 сценариях. Пара живёт, пока гонка молчит: в тик гонки запись гарнизона у хранителя снимает
    `garrisonOf.retainAll`, и флаг получает другое тело; флаг без тела не остаётся. Нужно ли правило (например, не
    прикалывать гарнизонного хранителем на чужом флаге)?
    Рекомендация: правила не вводить — цена пары лишь переход одного бойца в тик гонки; вернуться, если живьём
    против фермера `dblf` часть «разные» станет заметной долей `keep2`.
    Решено (20.09.2026): правила не вводить; условие возврата — названное выше, по прибору `dblf=` живьём.

11. **Набор командира по инерции (дефект 4, (а)).** В режиме боя без `fightOnNow` и при пустой армии `cmdDetach` никто
    не трогает; прибор `cmdinert=` на гейте 1 тик, живьём против MetalicaX#12 — 0. Чистка мёртвых (`Squads.pruneDead`) и
    выживание состава на ранних выходах (v465) сделаны; инерцию трогать?
    Рекомендация: не трогать, пока прибор не покажет заметных тиков живьём против фермера (там режим боя без огня
    длится).
    Решено (20.09.2026): не трогать; условие возврата — `cmdinert=` живьём против фермера.

12. **Ворота захвата по вчерашним и сегодняшним данным в одном тике (дефект 6).** Бегуны зовут `captureBlock` /
    `captureAllowed` с `Prev.exchange` (Missions.kt: оценка и всерьёз), армия, гонка и тактик — с `meas.view`
    (Strategist.kt ×3, Tactician.kt ×2). `view` читают ворота `rush` (stalled), `contact.mass` и `first.fight`
    (exchangeLive), `enough`, `parity` (lostRaceNow(view) по ourLostWindow и выбор пола PARITY_FLOOR_LOST / _STALLED).
    В тик, когда `stalled` включается или гаснет, бегун и армия получают РАЗНЫЕ ответы про один флаг; то же с
    `approachRate` и `farmerQuietNow`. Порядок конвейера вынужден: бегуны идут до армии, меры размена считает армия.
    Прибор (следующая версия): `capqua=вето/спрошено` и `capua=` — ответ ворот армии отдельно от `capqu=` / `capu=`,
    которые описывают ответ первому спросившему всерьёз, то есть бегуну; разница между ними и есть цена рассинхрона.
    Варианты: (А) группа мер размена (`stalled`, `exchangeLive`, `ourLostWindow`) считается ДО бегунов — ответ один
    на тик, `Prev.exchange` у бегунов не нужен; цена — перенос группы мер мира выше `runRunners` (они читают только
    истории `Memory` и хиты) и пересъёмка эталона; (Б) один ответ на тик: ответ ворот запоминается по паре «тик ×
    флаг» первым спросившим (бегуном, по вчерашним данным) и отдаётся армии тем же — дёшево, но закрепляет за армией
    вчерашние данные; (В) оставить как есть, если прибор покажет расхождение в доли процента.
    Рекомендация: (А), но только после того, как прибор назовёт долю тиков с разным ответом; если она мала — (В).
    Прибор снят (v467, `capdis=`): на гейте 210 пар «тик × флаг» с разным ответом из 50 289, спрошенных обоими
    (0,42 %), 169 из них в одном фермерском сценарии match28:farm+weak; знак — в 192 случаях бегуну разрешено, армии
    запрещено (`enough`, `first.fight`), в 18 наоборот; живьём против MetalicaX#12 (8 игр) — ноль на 1 848 пар.
    Рекомендация по числам: (В) — оставить как есть, прибор в строке; вернуться к (А), если живьём против фермера
    `capdis=` покажет заметную долю третьей части `capqua=`. Величины, которые стадии армии переписывают ниже бегунов и
    которые ворота читают (кроме `view`): `interceptFlagId`, `fightPackIds`, `objectiveFlagId`, `firstFightTick`,
    `kiteChaseSeen` — при выборе (А) переносить их вместе с мерами размена.
    Решено (20.09.2026): (В) — как есть, прибор в строке; условие возврата к (А) — доля `capdis=` в третьей части
    `capqua=` живьём против фермера.

13. **Липкость гарнизона-скаута (дефект 5) отвергнута гейтом в трёх редакциях** (v466): запись скаута, удержанная на
    весь матч (с закреплением `detach`, с закреплением `assign`, с правилом «флаг без его стволов в
    SCOUT_FLEE_TRIGGER»), стоит 9–16 тыс. очков в трёх лагерных сценариях (match29:camp его очки 6 509 → 11 124 …
    21 803, match34:camp 6 311 → 15 507 … 22 105), потому что закреплённый скаут перестаёт быть телом, которое
    паросочетание посылает за дорогим чужим флагом, когда свой уже наш; чинимая текучка — 10 смен и 29 потерь записи
    на 140 сценариев (`garscout=`). Оставить нелипкость как есть (замысел v337 / v341 стендом не подтверждён) или
    искать форму липкости, которая не отменяет паросочетание (например, липкость только пока свой флаг НЕ наш)?
    Рекомендация: оставить как есть; прибор `garscout=` в строке `t=` покажет, если живьём против фермера текучка
    окажется больше стендовой.
    Решено (20.09.2026): оставить нелипкость; условие возврата — `garscout=` живьём против фермера выше стендовых
    10 смен / 29 потерь на 140 сценариев.

14. **Правило перебоя «стрелок переходит к следующей цели» (дефект 7) — вводить ли** (v468, прибор `okill=` /
    `okkill=`). Правило v140 не действовало ни разу (книга писалась и не читалась) и дважды отвергнуто живьём — v140
    (2-6 и 2-6 против 10-22) и в составе v244 (0-8). Прибор: живьём против MetalicaX#12 (8 игр, 4-4) целиком лишних
    назначений 8 из 1 292 (0,6 %), урон сверх добивания 2 521 из 89 408 (2,8 %) — около одного выстрела на игру;
    премисса держится (24 из 24 целей, расписанных насмерть, погибли), то есть цена правила не в ложной книге, а в
    редкости предмета. На гейте 9,4 % урона и 1 526 лишних назначений из 26 220 — все по приказу командира
    (`killable` раздаётся каждому достающему стволу), но стендовый соперник гибнет от книги всегда, и выигрыш правила
    на гейте был бы свойством заглушки. Вводить ли насыщение в `commandFire` (по замыслу v244: цель, которую добивают
    уже назначенные стволы, следующему не даётся) или правило в `shoot`?
    Рекомендация: не вводить; прибор остаётся в строке. Вернуться, если против другого соперника `okill=` третья
    часть превысит единицы процентов расписанного урона при держащейся премиссе `okkill=`.
    Решено (20.09.2026): не вводить; условие возврата — названное выше, по `okill=` / `okkill=` живьём.

15. **Скаляры верхнего уровня после оборванного тика (дефект 13) — перечень.** `AbortRepair.repairFields` чинит
    словари и множества владельцев из списка; скаляр (`var` верхнего уровня файла или член объекта) оборванный тик не
    рвёт — он либо уже записан этим тиком, либо остался вчерашним, — но ПАРА скаляров, которую пишут разные стадии,
    остаётся в двух временах. Ниже — те, что пишутся по ходу тика и читаются следующим как «вчера»; накопители
    приборов (`srchTicks`, `cmdTicks`, `simErr*`, `mquiet*`, `radSum`…) и локальные `var` функций не в счёт.
    - **До стадий армии** (`readSignals`, мир — обрываются вместе с миром, то есть почти никогда): `noFireTicks`,
      `lastHurtTick`, `lastReachTick`, `fightImminentTicks`, `rushStartDist`, `fightMassedSeen`, `firstNearTick`,
      `kiteMassed`, `zeroLeadTicks`, `behindTicks`, `ourScore` / `enemyScore`, `ourDamageTaken` / `enemyDamageTaken`,
      `lastOurHits` / `lastEnemyHitsTotal`, `corneredWas`, `stallUntil`, `preyNearTicks`, `Signals.*` (восемь),
      `WorldState.ourRate` / `enemyRate` / `behindOnScore` / `flagFlipNow` / `flagsNow`.
    - **Меры армии** (`ArmyMeasures`): `firstFightTick`, `kiteChaseSeen`, `outmatchedTicks`, `lastDistanceKeptTick`.
    - **Стратег** (`ArmyStrategy`, пишут подстадии по порядку — обрыв между ними оставляет постуру нового тика при
      цели старого): `posture`, `postureSince`, `pushing`, `pushSince`, `pushHeld`, `leadHoldsWas`, `retreatTarget`,
      `evadeTarget`, `evadeEvaluatedAt`, `evadeLeft`, `escapeAt`, `idleDetachTicks`, `detachRecallTick`,
      `lastNonHuntTick`, `interceptFlagId`, `coreShortTicks`, `scatteredLatched`, `scatteredAtRelease`,
      `farmerOffTicks`, `standoffTicks`, `pressing`, `stalemateGap`, `objectiveFlagId`, `huntingThreat`,
      `fightPackIds`, `touchMin`, `touchShareLast` / `hisTouchShareLast` (Power.kt, пишет стойка), `Memory.prevPosture`,
      `Memory.postureCandidate` / `candidateSince`, `Memory.huntQuarry`.
    - **Тактик, строй, огонь**: `focusId`, `formWaitSince`, `TacticianState.focusPredDmg` / `huntsWounded`,
      `rangedLevelLatched`, `marchPrevSx` / `marchPrevSy`, `wallAddrPrev` / `wallLostPrev`, `yieldingTick`,
      `Wall.victimNow` / `victimSaveable` / `wallCells`, `goalTick` / `goalCx` / `goalCy` / `goalField` / `goalSeeds`,
      `FireBook.prevShooters`, `lastFireTick`, `Executor.ovw*`, `Arbiter.conf*`.
    - **Конец тика** (`RememberTick`, `Prev.*` — девять; обрыв ДО них оставляет весь `Prev` вчерашним целиком, что и
      есть согласованное «позавчера»): `Prev.exchange`, `ledgerWindow`, `hisLostWindow`, `approachRate`,
      `farmerQuietNow`, `cmdMode`, `touchShare`, `hisTouchShare`; `Memory.contactPrev` / `armyPrev` / `fracPrev` /
      `addrPrev`; `capquTick` / `capTick` (замки приборов — самочинятся сменой тика).
    Опасны не скаляры, а их ПАРЫ через границу стадий: (а) `posture` нового тика при `objectiveFlagId` и
    `interceptFlagId` старого (обрыв внутри стратега) — постура FLAG без цели читается тактиком как «цели нет»;
    (б) `focusId` нового тика при `FireBook.fireOf` старого — исполнитель огня читает приказ прошлого тика на цель,
    которой фокус уже не держит. Пара `fightMassedSeen` (сигналы) / `firstFightTick` (меры) согласована: первая
    считается по вчерашнему размену и от второй не зависит. Ни одна из двух пар не чинится списком владельцев: чинить
    можно только откатом всей группы к «вчера», то есть переносом группы в `Prev` целиком. Нужна ли починка скаляров
    (одна операция отката на обрыве — группы (а) и (б)), или довольно перечня: за матч обрывов 0–2 (`abort=` в
    строке), и обе пары самовосстанавливаются следующим тиком?
    Рекомендация: довольно перечня; вернуться, если `abort=` живьём станет больше единиц за матч.
    Решено (20.09.2026): довольно перечня; условие возврата — `abort=` больше единиц за матч живьём.

**Состояние работы (на момент записи).** Сделано и закоммичено на ветке `pain-and-gain` (в `main` не посажено —
посадка пачкой в конце): v462 (дефект 1, `escapeNeeded`), v463 (дефект 2, защёлка ротации: выход при погибших
лекарях, обе таблицы под чисткой), v464 (дефект 3, инварианты членства: состав гонки без отряжённых стратегом,
хранителей и преследователей; `sqref=`, `racex=`, `dblf=`), v465 (дефект 4, состав переживает ранний выход,
`pruneDead`; `raceexit=`, `cmdinert=`, `cmddead=`), v466 (дефект 5: прибор `garscout=`; липкость отвергнута гейтом —
вопрос 13). Живые замеры: по 8 тестовых игр против MetalicaX#12 на v462, v463 (до и после), v464, v465 (до); режим
пар против него не включается, поэтому дефекты 4 и 5 живьём на ботах тестового списка не экспонированы — их держит
гейт (сценарии фермера и призрачные записи). Эталон тождества — `runs/gate_465` (v466 на гейте равен ему плюс поле
`garscout=`); эталоны снимаются скриптом из проверочного ворктри по коммиту, живые серии — только при закрытом гейте,
оракул на загруженной машине — `JOBS=2 NOCLOCK=1`. Сделано после записи: v467 (дефект 6: прибор `capqua=` / `capua=` /
`capdis=`, вопрос 12 дополнен числами — рекомендация (В)), v468 (дефект 7: прибор `okill=` / `okkill=`, правило —
вопрос 14), v469 (дефект 8: `Orders.dismiss` в тик без армии, `cmdstale=`; стенд не экспонирован, живьём 1 тик в
двух проигранных играх серии v472), v470 (дефект 9: у `commandRace` снят мёртвый `out`, тождество), v471 (дефект 10:
`passKeeper` через `commit`; `pass=keeper` 52 клетки на гейте против 0 — вопрос 5 закрыт числом), v472 (дефект 11:
`guards` и `poker` на `!meleeOnlyLive`; гейт 122/140 со сдвигом в обе стороны, живьём 2-6 против MetalicaX#12 при
`guard=` без роста), v473 (прибор `gstrip=` к v472: на гейте `guards` раздетый мили не открывает никогда, `poker` —
206 крипо-тиков), v474 (дефект 12: `Prev.touchShareLast` — одно значение на тик; гейт 139/140, один призрак), v475
(дефект 14: `ourFireRecentPrev` / `hisHurtRecentNow`, тождество); дефект 13 — перечень, вопрос 15. Эталоны: `gate_465`
(v462–v471), `gate_472`, `gate_474`. Ветка перебазирована на `main` 2f7574b (две посадки tools/: `play.py --ab` брал
папку заготовки «advanced» с пустым `loop()` и не замечал пустой консоли — 17 рук A/B пропали). A/B v471 против v473
(правка дефекта 11) исправленным `--ab`: 5-3 и 5-3, `gstrip=0/25` за 8 игр — правка редка и исхода не меняет, остаётся
по механизму (вопрос 1 закрыт). Дальше — итоговый отчёт и посадка пачкой.
