package season4.painandgain

/**
 * АРБИТР, ВЕРСИЯ 0 (v238, этап 3 переработки): правила совместимости интентов пока только СЧИТАЮТ, ничего не снимая, —
 * чтобы до этапа 5 (приоритеты, снятие проигравшего предложения, движение через чистый решатель) было видно, сколько
 * конфликтов конвейер получает сегодня. Два счётчика в строке `t=`: `ovw=` — перезаписи слота (второй интент того же
 * семейства одному крипу за тик, считает [Executor]), `conf=` — интенты с целью вне досягаемости с текущей клетки
 * (правило R5 плана: движок такой интент отвергает молча).
 */
internal object Arbiter {
    var confReach = 0

    fun audit() { confReach += Executor.unreachable(RANGED_RANGE) }
}
