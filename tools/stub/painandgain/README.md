# Offline stub harness — Pain and Gain

Runs the compiled bot (`../../../build/js/.../season4/painandgain/PainAndGain.export.mjs`, i.e. the build of the
worktree this directory lives in) under Node against a stub of the Arena runtime: `game/` (constants, prototypes,
Dijkstra `searchPath`, simultaneous movement with swaps and chains, fatigue by part type, front-to-back part damage
and healing from the tail of the body, as in the engine), `arena/season_4/pain_and_gain/basic/prototypes.mjs`
(`ScoreFlag`), and `world.mjs` (flags, score, the global debuffs as `effects`, the early end on annihilation or an
unreachable lead). There are no spawns: both armies are placed at the start. What each scenario replays and why it
exists is in `docs/pain-and-gain.md`.

```shell
NODE=$(ls -d ~/.gradle/nodejs/node-*/bin/node | tail -1)                       # node is not on PATH here
$NODE --import ./register.mjs run.mjs 2000 rush                                 # synthetic point-symmetric map
MAP=map-match8.txt START=match2 $NODE --import ./register.mjs run.mjs 2000 hunter   # a live map; we are player 2
MAP=map-match6.txt $NODE --import ./register.mjs run.mjs 2000 sleeper           # a live map where we were player 1
SLEEP=300 MAP=map-match7.txt START=match2 $NODE --import ./register.mjs run.mjs 2000 sleeper   # the sleeper wakes at 300
TRACE=510-600 MAP=map-match11.txt $NODE --import ./register.mjs run.mjs 2000 sleeper   # per-tick positions of every creep in that tick range (reading a chase)
REPLAY=~/ScreepsArena/replays/<id>.replay.json.gz $NODE --import ./register.mjs run.mjs 2000 ghost   # the recorded opponent of a live match walks its replay (see below)
zsh regress.sh v9                                                               # every scenario, one line each, logs in out/
```

Maps `map-match1..25.txt` and `map-match28..35.txt` are the `DEBUG_MAP` dumps of the live matches, numbered by match — the gaps are matches whose map was one already here (`#` wall, `~` swamp, `m`/`e` the two armies'
start cells, `F` the flags). Enemy scripts: `none` (a dead bot), `scouts` (only the two scouts take flags), `grab`
(the army camps on our side), `rush` (everyone at the nearest of ours), `greedy`, `army` (the match-3 opponent: D5,
own A3, hover by its corner, engage within 20, healers adjacent, focus the lowest hits, sweep after we die), `hunter`
(match 4 and 8: D5 with the whole army, then straight at ours), `kite` (match 5: bait melee plus kiting ranged),
`sleeper` (idle until `SLEEP`, default 500, then rush), `nine` (match 9: straight at our army from tick 1, our
healers shot first, its own healers two cells behind its line and stepping away from our armed creeps), `twelve`
(match 12: D5, a loop through our half up to its H4 corner, then the match-9 hunt at full speed), `fourteen` (match
14: `nine` plus rotation — a fighter below half its hits walks to its healers, stays out of our armed creeps' reach and
returns above 0.9; its healers stand adjacent to the most damaged), `block` (matches 14–16: once hunting, the army
moves as one block — melee in the front row, ranged two cells behind the melee anchor, healers three behind — fires at
whatever is in range and rotates its damaged fighters back to the healers), `wing` (match 17: the block with its
ranged in the front row — the line stops three cells from our nearest creep and shoots, its melee only step to what
comes within two, a ranged with one of our armed creeps within two backs off two rows), `spread` (match 19: every enemy
creep takes a flag of its own, two per flag, sits on it, steps away from our armed creeps within six and comes back —
it never fights as an army and farms the flags' points), `scatter` (live match 240, ricardo18informatica2020's scatter from the first tick: by the replay, a melee and a ranged garrison his R3, a ranged and a healer the A3 nearer to us, a melee and a ranged the other A3, a trio takes D5 and then tours A3 -> A3 -> R3 -> D5 three hundred ticks at each, the last melee and ranged sit at the nearer A3 until t=400 and then join the R3 garrison, the runners take the two H4 corners and sit; nobody fires, a creep steps away from our armed creeps within six and comes back — six flags by t=86 against our one, D5 at 38, R3 at 43, both A3 at 53/56, both H4 at 82 on the stand as live), `brawl` (live matches 238 and 249, けろびー's blob at parity, both lost by annihilation: the whole army walks at ours as one blob and keeps its edge — it advances only formed, a melee holds the front and swings at what is adjacent, a ranged stands three from our nearest armed creep and at least three from every other and fires single shots at our armed ranged first, a healer one behind the nearest ranged unless a mate at three or more from our armed is wounded; at first contact the blob steps back a cell for three ticks and then comes on, as the live one did at t=44–49 of match 249. The stand also prints `ours act:` — the bot's own uptime counted the way `tools/replay.py` counts it live: healer creep-ticks adjacent to a wounded mate and heals, melee creep-ticks adjacent and swings, ranged creep-ticks with a target in three and shots — and `TRACE=t0-t1` now prints the enemy's bodies too. The form is not calibrated to the live outcome: the stand's bot destroys the blob on all eight maps in 280–540 ticks, where けろびー wiped it twice; what it does reproduce is our side's shape — melee adjacent 5–16 % of contact creep-ticks against 5 % live, healers 99 % against 94 %; the eight maps 28–35 are a measured family, not a gate line), `roost` (match 25: the same dispersal, but the creep never
leaves its flag — it does not step away from ours either; the live opponent of match 25 held all seven flags by t=80 and
never moved again, and our own log reported every combat enemy stationary), `farm` (live match 26: the army moves as
one blob to the flag nearest the blob that it does not own, and never engages — a creep with one of our armed creeps
within six steps away and comes back after; its two runners each sit on a flag. That match ended with both armies at
full strength, 902 hits of damage in 1500 ticks and not one death, and it won on points 23408:12721), `camp` (live match 70: the same farmer until nothing is left to take — a flag not its own and not occupied by one of ours — then the whole blob parks on the centre flag and never moves again, not even away from our armed creeps; live it sat there with twelve at 0.6 of its power for a thousand ticks while our army stood ten cells away without a shot; on maps where our army leaves flags unoccupied the blob keeps walking back onto them and the line is the farm race again). `camp+shy` (live matches 133, 152 and 159 — けろびー's camper: the parked blob steps aside when our armed creeps come within six, walks back onto the flag when they are gone, and never fires; the plain camp is destroyed by the push at t=400–700 and the live one was never reached, and silent it reproduces the live camper losses: on v70 maps 30 and 33 lost, 29, 31 and 34 won). `screen` (matches 29–30: the live block that wins an even fight — two rows fixed on its melee centroid, ranged-melee-ranged in front and the healers directly behind, moving as one block that advances only while formed; the line holds exactly three cells from our nearest armed creep and gives ground to one that steps to two, every gun goes to our most forward creep, healers stand adjacent to whoever is being hit, and its melee enter only on a creep its ranged have already disarmed. It beats the bot on map 30 (4 alive against 11) and reproduces the standing duel of match 30 on map 25; on maps 29 and 22 its formation gate deadlocks and it never engages — an open item of the script). `screen+fast` drops the screen's formation gate (the live line of match 78 walked at us at full speed and formed on arrival) — even so the stub's screen loses to the block, which the live one does not; the gap is open. `screen+focus` (matches 140–179: the live fighting line stands at three from our MOST FORWARD creep and every gun of its goes to that one creep, its melee at two in front of its ranged as a poke shield — four or more of its shots on one of ours in 11–28 % of its firing ticks live against 0–3 % of ours; in the stub the line does not hold the way the live one does — destroyed at t=180–210 on maps 25, 28, 30–32 and 34 with its four-plus share 0–17 % — so the standing line of those matches is still not modelled, an open item; 06.09.2026 its ranged also step back one cell from any of ours that comes within three — a greedy step to the free neighbour farthest from it, since a flee path with every creep at 255 finds nothing inside a formed line — and the line is a brawl all the same: its ranged at zero to two from ours in 75–100 % of contact samples, fights over by t=144–215 on five maps of six; later the same day its ranged also keep out of our ranged reach and its melee poke from two, stepping in, swinging and stepping back, and the outcomes did not move by a tick, so the brawl has a cause none of these touch). Any script takes `+flagless` (or `NORUNNERS=1`) to keep the enemy's runners idle: the live opponent of matches 30–31 took no flag until our army was dead, and `screen+flagless` is the closest model of him — v28 held its first flag against him and lost four armies, v29 evades and wins on that flag's points. `+weak` fields the enemy as a remnant of eight — two scouts, one melee, three ranged, two healers — the shape the live opponent of matches 28 and 33 was left with after our hunt, before it farmed the flags behind our back; `farm+weak` is that second half. Modes combine with `+`. The runner prints
a `cpu t=N: max=..ms at t=M slow(>50ms)=K` line every 100 ticks — the bot's `loop()` wall time in Node, a relative
measure of the arena's 100 ms tick budget (match 16 timed out three ticks in the thick of the fight). The bot itself
prints `bfs t=N max=K` every ten ticks — the most flow-field BFS runs in one tick since the previous line (the live
timeouts of matches 16–18 all fell inside flow-field BFS). At the end the runner prints an `enemy conc:` line — the enemy script's fire concentration as `tools/replay.py`'s conc counts it live: per tick the largest number of its single-target shots on one of ours, as a histogram and the share of firing ticks with four or more.

**`tour` — the farmer that keeps its distance (07.09.2026, MetalicaX of matches 258/274/277, 3-17 against us).** The `farm` blob with a twelve-cell step-away radius instead of six, firing at what comes within three, and with nothing left to take walking the flags in a cycle (a new one every 150 ticks) instead of parking: live it hovered at nine to thirteen at parity, the contact flag flickered, all twelve stayed "huntable", and its runners took the flags while our army faced it — the keeps-distance stall of v112 fired twice in 277 and the race was still lost on the detachment's release/recall and the FLAG/HOLD flicker on the approach rate. `tmp`-style family: maps 28–35, the `detach`/`stall` line counts and the score.

**`split` — the farmer in two groups (07.09.2026, けろびー of matches 294 and 296).** By the replay his nine armed walk as two blobs of six and seven, each farming a flag of its own, merging and splitting again — six flags by t=80 to our one while our "scattered" label (largest group at most half) never fires on seven of nine and the opening race never starts. Here the fighters alternate into two groups; each farms the nearest takeable flag from its own centroid (not the other group's), steps away from our armed within six and fires at what comes within three, as `farm`; the runners as `farm`. The family prints the flags at t=100 and t=200 with the score — the opening race is what it measures.

**`ghost` — a live replay drives the enemy (06.09.2026, tool 3 of the analysis set).** With `REPLAY=<id>.replay.json.gz` (arukuka's format, fetched as `tools/replay.py` describes; the files live in `~/ScreepsArena/replays/`) the map, the flags, both armies' bodies and start cells come from the replay — no `MAP`, no `START` — and every enemy creep walks, tick for tick, the cell the replay recorded for it: one move intent to the adjacent recorded cell, which the engine resolves as it would live (a creep of ours on that cell refuses the move; the ghost catches up when it frees, by a path step when two or more behind); fatigue is not modelled for ghosts. Fire and healing are by rule, not by record — the recorded targets stood where OUR recorded army stood, and the live bot is elsewhere: single-target at an armed ranged of ours first, then the lowest hits within three, melee at the adjacent lowest, healers on the most wounded mate within three. A ghost that outlives its record stands where it ended and keeps firing. The run ends with a `ghost <id>:` line (the recorded winner, ticks and deaths against the stand's outcome; ghosts off their recorded cell — 1–3 % of creep-ticks on the four records tried; OUR creeps off our own recorded cell, with the first tick it happened) and a `ghost entry` line: hits lost by each side over the first 20/50/100 ticks after the first contact, stand against record, plus `entry t=N:` lines for those thirty ticks intent by intent. **What it measures and what it does not**, from the four records it was built on (273 Coldkimchi line L, 267 and 251 けろびー blob L, 264 blob W): the OPENING and the ENTRY are faithful at the decision level — the same evade points to the cell ((10,71) -> (7,75) -> … -> (3,95) in 273), first contact at t=98 against the record's 98, 42 against 41, 101 against 100 — and the entry's first volleys are the record's; but the EXCHANGE is not: over the first twenty ticks his side lost 3 774 hits in the stand against 1 081 in the record (273), 3 791 against 1 441 (267), 3 114 against 726 (264) while ours lost about what it lost live, because our melee reached his line four times as often (20 swings against 5) — his recorded step-backs happened when OUR recorded melee pressed, not when the live ones do, and our own creeps part from our recorded cells from tick 1–2 (path tie-breaks: a scout at (11,10) where the record has (12,10)), 90 % of creep-ticks off by a cell or two, so a kiting line is walked into. Two reaction rules were tried and REJECTED with numbers: `+shy` (a ranged or healer with our armed melee within two steps back, as every recorded line does) made it worse — 6 970 lost in twenty ticks in 273, 4 476 in 267 — and `+nurse` (a healer with a wounded mate within three and none adjacent steps to it) likewise, 5 462 and 3 693; both stay as options for the next attempt. A record whose opening our current build no longer repeats (251: v101's opening; the ghost blob marched to where we WERE and met nobody until t=271 against the record's 63) measures nothing past the `ghost entry` line saying so. Use: does the current build take the recorded entry (contact tick, place, first volleys), and by how much does the exchange part from the record — not who wins.

**Two lines every run prints since 07.09.2026.** `entry: contact t=N hits lost ours/his +20 a/b +50 a/b +100 a/b` — the first tick a fighter of each side stands within three of the other's and the hits each side lost over the next 20/50/100 ticks: the fight's quality as a number, since the outcome (the stand's blobs always die) does not carry it; `tmp`-style tables of it across the fight scenarios are how v109–v111 were judged. And in `ours act:` two more counters — `his focus target healed` (per tick the creep of ours his single-target intents put the most damage on: did a heal of ours land on it) and `a healer adjacent to it`; live those were 2 of 19 and 0 of 20 ticks in the blob losses 267 and 264.

`regress.sh` runs its scenarios **in parallel**, `JOBS` at a time (default 8, `JOBS=14 zsh regress.sh tag` to widen):
each scenario is its own node process with its own map, log and result file, and the report is put back in order at the
end. Serially the suite took over three minutes; parallel it is under a minute, and the report is byte-identical. This
is not only about impatience — `tools/land.sh` runs every arena's suite one after another, so a slow suite here delays
the landing of every other arena. Logs still land in `out/`, one file per scenario per tag; a full run writes a few
hundred megabytes there, so `out/` is worth emptying between investigations (it is gitignored and every log is
reproducible by re-running its scenario).

`errors` counts `loop error` lines and, since 05.09.2026, every crash trace the bot's `runWithSourceMapSupport` prints (one per `at captureStack` frame): a `NoSuchElementException` thrown every tick for 1500 ticks used to be reported as `errors: 0`, because the mapped trace starts with an empty message line, not with `loop error`.

**Three tiers since 05.09.2026.** `run` lines are the gate (120): every line that has ever failed in the recorded runs, every live-opponent script (`screen`, `screen+flagless`, `nine+flagless`, `block+flagless`, `farm+weak`, `twelve`, `grab`, `scouts`, `none`) and every line of the recent maps 28–35. `grind` lines (184) are the generic scripts on the older maps that never failed once, plus the known points races — `zsh regress.sh <tag>` still runs all 304 (~3.5 min), `zsh regress.sh gate` and the landing gate run the 120 (~1.5 min). Fifteen full runs in one day, each reading the same 184 lines that never change, was the operator's prompt; a line that fails in the grind is promoted by moving it back to `run`.

`tools/land.sh` runs `regress.sh` as the landing gate: every line must say `PASS` with `errors: 0` — the enemy army
destroyed, or the match ended with our score ahead. A new arena's harness starts as a copy of a sibling directory
(`game/` is arena-agnostic; the runner and `world.mjs` are where the arena rules live).

**`brawl+dart` (07.09.2026).** The brawl's melee as the replays of the series of twenty count them (197ebb, けろびー#1): a soft target within THREE draws a melee, and a melee that was adjacent last tick steps back a cell when one of our armed creeps is within two — adjacency in one-tick darts (live: at one, two and three cells 65:60:69 creep-ticks, adjacent 27 %, 53 swings to our 11). On the stand the build still destroys it on all eight maps with the entry ours on six — the live blob's edge is not yet in any form; the row is kept as the measure for melee rules (v122: m30 +50 5 054:8 034 -> 3 208:10 352).

**`blitz` (07.09.2026).** けろびー#4, the fast tourer of matches 327–346 (three losses on points with the fight won): the `split` farmer that keeps what it takes and takes what is ours. Two groups by alternation sweep the nearest flag that is not theirs — ours with a creep on it included — at full speed (replay 197ed2: R3b and D5 at t=39, both A3 at 57–58, both H4 at 81–82); the group's leader steps onto the flag and stays as its keeper (steps away from our armed within six, comes back), the rest move on; the two scouts run for the farthest free flags. Against v122 it is 4-4 with the flags 1:5 to 2:4 at t=100 and 1:5 to 1:6 at t=200 — the live opening to the number; the losses are on points with our army intact (m28 3 961:23 980).

**`screen+focus+poke` (07.09.2026).** The line's fighters rotate like Coldkimchi's: a melee below half its ATTACK parts (a ranged below half its RANGED parts) steps out of our reach to its nearest healer and returns at five of eight. Without it the poke melee stood at two from our front and were stripped where they stood (m29: his four melee gone by t=50, the line destroyed at t=129); with it the line lasts longer (m35 219 -> 414, m29/m30/m34 end as his lead-proof remnants of 2–3) and our reach share against it stays below his (13–37 % to his 28–60 %), but the entry is still ours three to one (+20: 1 918–4 023 lost to his 9 389–11 978) where the live line loses 1 256–1 946 to our 2 983–4 112. The form is a step toward the live line, not the line: what it still lacks is the entry — the live line's creeps take our first volleys without walking into our block one by one. Second cut, the same day: under `+poke` the line's ranged no longer step away from our ranged within three — the live line's ranged stand level with its melee, a 4x4 square three from our nearest creep all through the entry (506, t=80–92) — and the entry is still ours three to one (+20: 2 140–4 034 to his 9 771–12 688, the line gone by t=119–302). So the gap is not the ranged's distance: what the stand's line lacks is what the live one does with its healers and its fire — the focus target healed 52 % of the time live against 36 % here, four guns on one creep — and that is the next thing to copy. Third cut: the stand now prints the mirror of that measure — OUR focus target healed by him, his healer adjacent to it — and under `+poke` his healers walk to the creep that lost hits this tick and heal it first. The trace says the mechanism works (a healer at one or two from the creep under our fire within three ticks) and the number does not move: his focus target healed 2–14 % of its damage ticks against 52 % live, because our fire on it is 360–560 a tick — the poke melee that steps in to one dies in two ticks under our four melee, where the live line's melee are adjacent to ours 3 % of the time. The gap is how his poke avoids our melee while reaching our ranged, not the healing. The line-against-the-poke rule of the bot (v126, OFF) waits for that.
